<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TODO</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        footer {
            background-color: #27272a;
            color: white;
            text-align: center;
            padding: 1rem 0;
            position: relative;
            width: 100%;
            bottom: 0;
        }
    </style>
</head>
<body class="flex flex-col min-h-screen bg-zinc-900">
    <h1 class="text-center text-white py-4 bg-zinc-800 cursor-pointer">TODO</h1>
    
    <div class="main flex-grow w-full flex flex-col items-center">
        <div class="form p-6 text-white w-full max-w-lg">
            <form action="/create" method="POST">
                <label for="title" class="sr-only">Title</label>
                <input id="title" name="title" class="block w-full rounded-md outline-none py-2 resize-none bg-zinc-800 mt-2 p-4" type="text" placeholder="Title goes here..." required>
                
                <label for="details" class="sr-only">Task Details</label>
                <textarea id="details" name="details" class="block w-full rounded-md outline-none resize-none py-2 bg-zinc-800 mt-2 p-4" placeholder="Write your task detail here..." required></textarea>
                
                <input class="mt-4 bg-blue-600 hover:bg-blue-400 text-white px-5 rounded-md py-2 cursor-pointer w-full" type="submit" value="Create Task">
            </form>
        </div>

        <div class="tasks flex flex-col items-center p-10 w-full">
            <% if (files.length > 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="task w-full max-w-lg mt-6 p-6 rounded-md bg-zinc-800">
                        <h1 class="text-white text-xl tracking-tight"><%= val.replace('.txt', '') %></h1>
                        <a href="/task/<%= val %>" class="text-blue-500 inline-block mt-3">Read more</a>
                        <div class="actions mt-4 flex space-x-4">
                            <a href="/edit/<%= val %>" class="bg-blue-600 hover:bg-blue-400 text-white px-5 py-2 rounded-md">Edit</a>
                            <form action="/delete/<%= val %>" method="POST" class="inline-block">
                                <button type="submit" class="bg-blue-600 hover:bg-blue-400 text-white px-5 py-2 rounded-md">Delete</button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <h1 class="text-white">No tasks yet.</h1>
            <% } %>
        </div>
    </div>
    
    <footer class="bg-zinc-800 text-white text-center py-4">
        © 2024 TODO BY VISHNU GUPTA
    </footer>
</body>
</html>
